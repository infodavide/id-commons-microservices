# Need by app artifact
spring.main.allow-bean-definition-overriding=true
# Disable to avoid logging system failure on restart
spring.devtools.restart.enabled=false
# Use true to allow basic authentication, allowed by default
authentication.basic.allowed=true
